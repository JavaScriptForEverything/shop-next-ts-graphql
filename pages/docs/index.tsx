import { withDocsPanel } from '@/shared/hoc/withDocsPanel'
import { Button } from '@mui/material'

const Home = () => {
	return (
		<>
			<p>Docs Home page</p>

		</>
	)
}
export default withDocsPanel(Home)
